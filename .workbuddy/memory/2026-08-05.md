# 2026-08-05 工作日志

## 项目首次探索：OncoPath (LungEvidence)

- **项目定位**：基于证据的肿瘤学 AI Agent 平台，专注肺癌（NSCLC 优先）
- **核心原则**：Evidence-First, AI-Second；临床规则硬编码，LLM 不做诊断预测，RAG 检索文献支撑
- **技术栈**：
  - 前端 `app/`：Next.js 16.2 + React 19 + TailwindCSS v4
  - 后端 `backend/`：FastAPI 0.109 + SQLAlchemy 2.0 + Pydantic + OpenAI SDK
  - 数据库：PostgreSQL + pgvector（生产）/ SQLite（MVP）
  - 检索：PubMed (biopython) + Europe PMC
  - 部署：docker-compose（前端 38030 / 后端 38080 / pg 35432 / redis 36379）
- **后端模块**：API(auth/cases/evidence/analysis/stats/health)；services(agent/rules/vector_search/pubmed/europe_pmc)
- **文档库**：`docs/` 50 份设计文档，覆盖愿景、架构、RAG、医学合规、MVP 路线图
- **Roadmap**：3 阶段（NSCLC MVP → 知识图谱+指南 → 多疾病扩展+EHR 集成）
- **待注意**：
  - Next.js 16.2 / React 19 版本很新，可能有生态兼容问题
  - 根目录 `mGGO肺癌预后分析.md`（350KB）是肺癌预后分析资料
  - `app_src/` 是前端 4 个核心文件的源备份
  - `data/evidence_database.json` 是初始证据数据集

## 备注
- 用户首次交互时跳过了身份确认（bootstrap），保持 WorkBuddy 默认身份，未修改 SOUL.md/IDENTITY.md/USER.md，BOOTSTRAP.md 保留待后续
- 沟通风格暂按简洁专业处理

## 功能完成度深度审查（用户问"功能是否完善"）

审查方法：Explore agent 全面走读 + 自己读 agent.py/rules.py 交叉验证。

### 整体判断：处于「可演示原型→真 MVP」过渡期，4 个关键断层

### 已可用（绿）
- 文献抓取 pubmed.py / europe_pmc.py：真实实现
- 用户认证 auth.py：JWT+bcrypt+DB 完整
- 前端报告页 page.tsx（692行）：状态机 + EvidenceReport 双轨
- Docker 部署：compose 完整

### 有缺陷（黄）
- rules.py：T 分期准（含 subsolid 用 solid_size），但 N1/N2/N3 全归 "Advanced (II-III)"（rules.py:53-54），未细分 II/III 期
- agent.py：实现完整（严格安全 prompt + JSON schema），但 .env 用 sk-mock-key-for-development，实际调 OpenAI 必失败
- cases.py：仅 GET，无 POST/PUT/DELETE

### 关键断层（红）
1. **RAG 向量检索死代码**（最严重）：VectorSearchService 全库零 import；get_embedding 无 key 返回 [0.0]*1536；依赖的 embedding 列在 models/evidence.py 中不存在，设计断层；/evidence/search 退化为 ILIKE
2. **证据库未灌入**：data/evidence_database.json 有 9 篇真实论文（带 DOI），但 /seed 只灌 3 条 mock
3. **统计注水**：stats.py:31-33 叠加 base_evidence=40/base_patients=187450，注释自承"make platform look populated"
4. **知识图谱假静态**：KnowledgeMapPreview.tsx 10 节点全硬编码，"每日自动更新 PubMed"宣传为假

### 修复优先级
- P0：修 vector_search（Evidence 加 embedding 列 + 迁移脚本灌 9 篇 + 接入 /search）；换真 OpenAI key 验证 LLM
- P1：stats.py 去膨胀基数；rules.py 补 N 分期
- P2：cases.py 补 CRUD；KnowledgeMapPreview 接真实数据

## P0 完成：RAG 向量检索打通 + 灌 9 篇真实论文

### 改动文件
1. `backend/app/models/evidence.py`：加 `embedding`(Text, JSON序列化向量) + `url`(String) 列。用 Text 而非 pgvector Vector，兼容 SQLite MVP + PG，应用层算相似度
2. `backend/app/schemas/evidence.py`：EvidenceResponse 加 `url: Optional[str]`
3. `backend/app/services/vector_search.py`：重写。get_embedding 调 OpenAI ada-002（无 key/mock 返回零向量）；search_evidence 用应用层余弦相似度（JSON 反序列化 embedding 算 cosine），无可用 embedding 时退化为关键词 ILIKE。修复原 SQL select 了不存在的 url 列的 bug
4. `backend/app/services/seeding.py`（新建）：map_study_to_evidence 字段映射 + seed_real_evidence(db) 幂等灌库逻辑。9 篇论文字段：title/journal/year/patient_n/study_type/doi/pubmed_id/key_conclusions/relevant_factors → 映射 authors(N/A+PubMed id)/summary(拼接)/conclusion(key_conclusions)/keywords(relevant_factors)/url(PubMed 链接)
5. `backend/seed_evidence.py`（新建）：独立灌库脚本
6. `backend/app/api/evidence.py`：/search 从 ILIKE 改为 VectorSearchService.search_evidence；加 /seed-real POST 端点
7. `backend/app/api/analysis.py`：Step 3 从多关键词 ILIKE 改为构造语义 query + VectorSearchService
8. `backend/requirements.txt`：加 `bcrypt==4.1.2`（修复 passlib 1.7.4 + bcrypt 5.0 不兼容 bug，bcrypt 5.0 在 detect_wrap_bug 检测时因 >72 字节密码抛 ValueError，降级到 4.1.2 解决）

### 验证结果（mock key 模式，无真 OpenAI key）
- DB 表结构：evidences 表含 embedding/url 新列 ✓
- 灌库：9 篇真实论文入库，带 PubMed URL ✓
- /api/evidence/search?q=STAS：返回 2 篇（关键词兜底，零向量自动退化）✓
- /api/evidence/seed-real：幂等（重复调用 added=0,skipped=9）✓
- /api/auth/register+login：JWT 正常 ✓（bcrypt 修复后）
- /api/analysis/generate：完整链路执行（rules算TNM→向量检索关键词兜底→LLM mock 分支返回提示文案）✓

### 待用户操作
- 要激活真实语义检索 + AI 报告：在 `backend/.env` 填真实 `OPENAI_API_KEY`，重跑 `python seed_evidence.py` 生成真实 embedding，重启后端
- pgvector 原生索引优化（生产级）留作 P1 后续

### 可复用模式（值得沉淀）
- 应用层余弦相似度方案：embedding 存 JSON Text 列，不依赖 pgvector 扩展，SQLite+PG 通用，MVP 友好
- 灌库脚本幂等模式：按 title 去重，可重复运行
- passlib+bcrypt 兼容坑：bcrypt 必须 ≤4.x，5.0 会崩
